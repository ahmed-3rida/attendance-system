# ✨ ميزة إدارة المراحل الدراسية

## 📋 الوصف
تم إضافة نظام إدارة المراحل الدراسية (Levels) للتطبيق، حيث يتم تصنيف المواد حسب المرحلة الدراسية.

## 🎯 الميزات المضافة

### 1. **جدول المراحل في قاعدة البيانات**
- تم إنشاء جدول `levels` يحتوي على:
  - `id`: المعرف الفريد
  - `name`: اسم المرحلة (مثل: المرحلة الأولى، الثانية، إلخ)
  - `description`: وصف المرحلة
  - `order_number`: رقم ترتيب المرحلة
  - `is_active`: حالة المرحلة (نشط/معطل)
  - `created_at`: تاريخ الإنشاء

### 2. **ربط المواد بالمراحل**
- تم إضافة عمود `level_id` في جدول `subjects`
- كل مادة الآن يجب أن تكون مرتبطة بمرحلة دراسية

### 3. **APIs الجديدة**

#### **المراحل (Levels APIs)**
- `GET /api/admin/levels` - جلب جميع المراحل
- `POST /api/admin/levels` - إضافة مرحلة جديدة (Super Admin فقط)
- `PUT /api/admin/levels/:id` - تعديل مرحلة (Super Admin فقط)
- `DELETE /api/admin/levels/:id` - حذف مرحلة (Super Admin فقط)
- `GET /api/admin/levels/:id/subjects` - جلب مواد مرحلة معينة

#### **المواد (Subjects APIs - محدثة)**
- `POST /api/admin/subjects` - إضافة مادة جديدة (يتطلب `level_id`)
- `PUT /api/admin/subjects/:id` - تعديل مادة (يتطلب `level_id`)
- `DELETE /api/admin/subjects/:id` - حذف مادة (مع فحص التبعيات)

### 4. **واجهة المستخدم**

#### **صفحة إدارة المراحل** (Super Admin فقط)
- **الموقع**: لوحة التحكم > إدارة المراحل
- **الإمكانيات**:
  - عرض جميع المراحل في جدول مع البيانات الكاملة
  - إضافة مرحلة جديدة
  - تعديل بيانات المرحلة
  - حذف المرحلة (مع فحص وجود مواد مرتبطة)
  - عرض المواد التابعة لكل مرحلة

#### **تحديثات صفحة المواد**
- عند إضافة أو تعديل مادة، يجب اختيار المرحلة الدراسية
- عرض اسم المرحلة مع بيانات المادة
- ترتيب المواد حسب المرحلة

### 5. **البيانات التجريبية**
تم إضافة 4 مراحل افتراضية عند بداية التشغيل:
1. **المرحلة الأولى** - السنة الدراسية الأولى
2. **المرحلة الثانية** - السنة الدراسية الثانية
3. **المرحلة الثالثة** - السنة الدراسية الثالثة
4. **المرحلة الرابعة** - السنة الدراسية الرابعة

## 📝 كيفية الاستخدام

### للـ Super Admin:

1. **إضافة مرحلة جديدة**:
   - اذهب إلى "إدارة المراحل"
   - اضغط على "إضافة مرحلة جديدة"
   - أدخل اسم المرحلة والوصف ورقم الترتيب
   - اضغط "إضافة"

2. **تعديل مرحلة**:
   - في جدول المراحل، اضغط على زر "تعديل" (✏️)
   - عدّل البيانات
   - اضغط "حفظ التعديلات"

3. **حذف مرحلة**:
   - في جدول المراحل، اضغط على زر "حذف" (🗑️)
   - تأكيد الحذف
   - **ملاحظة**: لا يمكن حذف مرحلة تحتوي على مواد

4. **عرض مواد المرحلة**:
   - في جدول المراحل، اضغط على زر "عرض المواد"
   - سيظهر modal بجميع المواد التابعة للمرحلة

5. **إضافة مادة مع المرحلة**:
   - اذهب إلى "إدارة المواد"
   - عند إضافة مادة جديدة، اختر المرحلة الدراسية من القائمة المنسدلة
   - **ملاحظة**: اختيار المرحلة **إلزامي**

## 🔧 التحديثات التقنية

### قاعدة البيانات:
```sql
-- جدول المراحل
CREATE TABLE levels (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    description TEXT,
    order_number INTEGER,
    is_active BOOLEAN DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- تحديث جدول المواد
ALTER TABLE subjects ADD COLUMN level_id INTEGER;
```

### الحماية والتحقق:
- ✅ فحص صلاحيات Super Admin لجميع عمليات المراحل
- ✅ التحقق من وجود اسم المرحلة (إلزامي)
- ✅ التحقق من عدم تكرار اسم المرحلة
- ✅ منع حذف مرحلة تحتوي على مواد
- ✅ منع حذف مادة تحتوي على محاضرات أو دكاترة مخصصين

### الترتيب:
- المراحل تُرتّب حسب `order_number` ثم `name`
- المواد تُرتّب حسب ترتيب المرحلة ثم اسم المادة

## 🎨 تحسينات واجهة المستخدم:
- عنصر جديد في القائمة الجانبية: "إدارة المراحل"
- أيقونة جديدة: `fa-layer-group`
- نماذج (Modals) مخصصة لإضافة وتعديل المراحل
- جدول تفاعلي لعرض المراحل
- إشعارات للعمليات الناجحة والفاشلة

## 🔄 التحديثات المستقبلية المقترحة:
- [ ] إضافة إحصائيات لكل مرحلة (عدد المحاضرات، الحضور، إلخ)
- [ ] تصفية المواد حسب المرحلة في صفحة المواد
- [ ] تصدير بيانات المراحل إلى Excel
- [ ] إضافة ألوان مميزة لكل مرحلة
- [ ] إمكانية إعادة ترتيب المراحل بالسحب والإفلات

## 📞 الدعم
في حالة وجود أي مشاكل أو استفسارات، يرجى التواصل مع المطور.

---

**تاريخ الإضافة**: 2025-10-21  
**الإصدار**: 2.0.0

